Jotting these down now - Things to consider for future iterations:

DONE:
[x] Making AI coach the first tab, consolidated Checkpoints + Faults & drills into "Golf Swing Mechanics"
[x] Removed auto-detect entirely (motion-energy + MediaPipe pose, spike graph) in favor of manual swing start/end marking + keyframe-dedup export sampling
[x] Step-by-step "How to use" instructions (dismissible banner, first load)
[x] Cropping video based on swing start/end inputs - satisfied by the existing "Loop swing" button (restricts playback/review to the marked bounds); trimming/downloading an actual shorter video file was not the intent
[x] Move API Key for AI Coach into a settings page - new "Settings" tab; AI coach tab shows a link there if no key is set
[x] One click export/copy/launch to claude app - added a "📤 Share to Claude" button using navigator.share() (Web Share API): builds the frame sheet + prompt and hands them to the OS share sheet in one tap, so Claude can be picked directly if installed as a share target. Only shown on devices/browsers that support file sharing (mainly mobile); falls back to the existing download+copy flow elsewhere. No way to auto pre-fill a claude.ai chat via URL exists, so this is the closest one-tap path achievable with standard web APIs - not yet verified on a real device with the Claude app installed.
[x] Text field to give claude more context (misses, focus areas, etc.) - the Session notes field now feeds BOTH the keyless chat-export prompt AND the direct AI Coach API call (runAnalysis), and it's surfaced at the top of the AI coach tab ("What are you working on?") instead of only living in Session plan, so users actually discover it. (The direct-API path is currently hidden behind a flag until user API keys ship, but the notes wiring is in place for when it returns.)
[x] Export with markups (drawn lines/angles/circles baked into an exported image) - shipped BIGGER than the original "current frame only" scope below: drawn shapes now bake onto EVERY frame of the automated multi-frame sheet export (on by default; a toggle appears when shapes exist so you can also export a clean sheet), so a plane/spine reference line carries across the whole swing for comparison. Only touches sheet rendering - the frame-extraction pipeline is untouched. Baked markups are also announced to Claude in the chat prompt. (The single-frame "Save frame" annotated export already existed.)
[x] Golfer handedness (Righty/Lefty) - the analysis agent was misreading a lefty swing as righty. Added a persisted toggle next to the view selector; handedness now flows into the frame-sheet header, the chat-export prompt, and the direct AI-coach prompt (with an explicit mirror instruction for lefties). Chose a one-tap field over auto-detection, which is unreliable in the down-the-line view (side of the target line the camera sits on flips the apparent handedness).
[x] iOS audio impact detection - on-device exports had never actually run the audio impact detector: Safari's decodeAudioData refuses to demux audio out of .mov/.mp4 containers (Chrome does, so it passed every desktop test). Now extracts the AAC track from the container and rewraps it as ADTS, which Safari decodes. Also adopted the analysis agent's validated 2kHz-highpass detector + a 5-clip ground-truth regression suite wired into CI.

REMAINING (re-estimated after checking the code):

1. Speed/FPS - are these being used?
   - Answered: yes, both actively used. `speed` drives actual video.playbackRate; `fps` drives the
     frame-step increment (Frame buttons) and is included as clip context in the AI/chat prompt.
     Nothing to clean up.

2. Mobile: stuck on "Loading" until you tap play (first frame doesn't paint)
   - Real bug. There's already a primeFirstFrame() mechanism meant to solve exactly this (muted
     play-one-frame-pause-rewind), and it's apparently not fully reliable on mobile. iOS Safari
     video-decode timing is input-dependent - needs on-device iteration, can't be fully verified
     from a dev environment alone.
   - Size: medium

3. Make the "How to use" intro banner dismiss permanently after first view
   - Blocked on real persistence (Supabase, roadmap item) rather than an ad-hoc localStorage flag.
   - Size: small once persistence exists; not worth doing standalone before then

4. Allow for simultaneous DTL and face-on video analysis into claude
   - Bigger than it sounds: the app currently holds one video/marker/shape set at a time - the
     DTL/face-on toggle only switches which reference content (checkpoints/faults) displays, not
     actual dual-video support. Would need real dual-video state, independent swing bounds per
     angle, and a combined AI prompt.
   - Size: large - worth its own planning session
